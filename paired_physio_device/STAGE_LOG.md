# Stage Log

Running log of what has actually been executed, in order, with job IDs and outcomes. Supplements `EXPERIMENT_PLAN.md`'s stage ledger with the exact provenance trail. Written for a resumed session to pick up accurately without re-deriving context.

## Session: 2026-08-20, this redesign's first implementation pass

1. **Read all three governing documents in full**: `CLAUDE_CODE_MASTER_PROMPT.md`, `Paired Device Experiments.md`, `Four-GPU, GPU-Only Research Execution.md` (`claude_pairphysnet_skills_and_prompt/`).
2. **Created the `paired_physio_device/` output tree** (Section Q).
3. **GPU preflight** (job 1611): real cluster topology confirmed — 8 physical RTX PRO 6000 Blackwell GPUs, MIG-partitioned into 32×24GB slices; a single job sees exactly 1 slice. `torchaudio` missing from venv (not needed — project uses librosa); `cuml` not in main venv (uses the established isolated-venv bridge instead).
4. **Stage 0 audit**: inspected raw dataset directly (50 subject folders, WAV headers via `wave`, `annotation.json` structure, SpO2/HR/sleep-stage CSVs). Built `audit/raw_audio_inventory.csv` (145 files) and `audit/pair_inventory.csv` (10,325 real same-event R/S pairs) via `scripts/build_audit_inventories.py`. **Two decisive data-integrity findings**, both auto-corrected into `EXPERIMENT_PLAN.md`: (a) no OA/CA/MA subtype exists in the raw annotations — only `{osa, hypo}`; (b) AHI≥15 has only 7 negative-class subjects (of 41) — AHI≥30 promoted to primary night-level target.
5. **Stage 2**: `scripts/validate_pairing_and_folds.py` — all 5 leakage/pairing checks passed on real data (report: `audit/pairing_fold_validation_report.json`). `scripts/paired_dataset.py` — real `PairedEventDataset`, smoke-tested end-to-end (loaded real R/S audio pairs, confirmed finite, correct shapes).
6. **Stage 3** (Section G): `scripts/paired_device_shift_analysis.py`, run on 450 real stratified same-event pairs. Window-level Mann-Whitney + Cliff's delta AND, new this pass, a **subject-level Wilcoxon signed-rank test (n=40 subjects)** — significant for all 6 acoustic statistics (p ≤ 3.9e-7). Output: `results/physiology/paired_device_shift_measures.csv`, `results/physiology/paired_device_shift_summary.json`.
7. **Section D2 prerequisite**: `scripts/audit_spo2_event_timing.py` — real event-to-nadir SpO2 timing distribution (9,935 events, 50 subjects). Median delay 41s (hypo) / 53s (osa); 150s window covers >99%. Output: `results/physiology/spo2_event_timing_audit.json`. Satisfies the master prompt's explicit "do not use an arbitrary fixed lag without auditing the actual distribution."
8. **Stage 4** (Section H, mandatory device probe): `scripts/run_device_probe.py` + sbatch. Discovered a per-submission SLURM QOS array-size cap empirically (`QOSMaxSubmitJobPerUserLimit`, ceiling ≈4 concurrent submitted jobs) — worked around via `scripts/submit_device_probes_throttled.sh`, a self-throttling background driver. **35/35 jobs completed, 0 errors** (jobs 1613, 1620-1653). Result: device identity recoverable at **balanced accuracy 0.93–0.995, AUROC 0.98–0.9996 across all 7 frozen encoders** (`results/device_probe/device_probe_summary.csv`) — the single most decisive finding of this session.
9. **Stage 5**: `models/pairphysnet.py` — full PairPhysNet (SharedEncoder w/ chunking + partial fine-tuning, configurable Pooling {mean, mean_std, attentive_stat}, ProjectionHead, GRL, NT-Xent pair loss, normalized-cross-covariance disentanglement loss, SpO2 aux heads). `scripts/test_pairphysnet_forward.py` (job 1654) — **PASSED**: real forward+backward on real paired audio, all outputs finite, gradients confirmed reaching every intended component.
10. **Stage 6, script + first smoke test**: `scripts/run_pairphysnet_training.py` (full A1-A5 training loop, validation-only model selection, MASTER_RESULTS.csv-compatible output schema). First smoke test (job 1655) hit a **real CUDA OOM** (batch_size=8, 24GB MIG slice, ~22.9GB peak). Fixed per the Four-GPU skill's own failure policy: gradient checkpointing (`use_reentrant=False`), batch_size 8→4 with 2-step grad accumulation, mixed precision (autocast + GradScaler) in both train and eval loops. Re-submitted as job 1656 — **outcome pending at time of writing this log; check `logs/slurm_ppn_train_smoketest_1656.out/.err` and this file's next update.**

## Not yet started (see `EXPERIMENT_PLAN.md` ledger)

Stage 6's real 25-job (5 variants × 5 folds) matrix (prepared: `scripts/run_pairphysnet_training_single.sbatch`, NOT submitted — genuinely multi-hour per job, held for confirmation once the OOM fix is confirmed working); Stage 7 (equal-data pooled controls); Stage 8 (subtype/error/case-atlas — confirmed `results/P0_device_gap` completion.json files do NOT store per-window predictions, so a real subtype breakdown needs either new A0 inference logging or Stage-6 models, not just aggregation of what already exists); Stage 9 (night-level screening); Stage 10 (statistics + multiple-comparison correction); Stages 11-13 (results audit, figures, journal paper — explicitly deferred, user said "do not generate the manuscript yet").
